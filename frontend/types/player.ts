export interface PlayerData {
    id: string;
    x: number;
    y: number;
    username: string;
}

export interface MoveData {
    id: string
    x: number
    y: number
}

export interface UserData {
    userId: string;
    username: string;
    password: string;
}